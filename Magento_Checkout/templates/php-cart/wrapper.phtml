<?php
/**
 * Hyvä Themes - https://hyva.io
 * Copyright © Hyvä Themes. All rights reserved.
 * See COPYING.txt for license details.
 */

declare(strict_types=1);

use Magento\Checkout\Block\Cart;
use Magento\Framework\Escaper;

/** @var Cart $block */
/** @var Escaper $escaper */
?>

<div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">
    <!-- Left Column: Cart Items (50% width) -->
    <div class="w-full order-2 lg:order-1">
        <?= $block->getChildHtml('form_before') ?>
        <?= $block->getChildHtml('cart-items') ?>
        <?= $block->getChildHtml('checkout_cart_widget') ?>
    </div>

    <!-- Right Column: Summary & Tools (50% width) -->
    <div class="w-full lg:sticky lg:top-4 order-1 lg:order-2 space-y-6">
        <!-- Coupon Code -->
        <?php if ($couponBlock = $block->getLayout()->getBlock('checkout.cart.coupon')): ?>
            <div class="card bg-white p-6 rounded-xl shadow-sm border border-gray-200">
                <?= $couponBlock->toHtml() ?>
            </div>
        <?php endif; ?>

        <!-- Shipping Estimation -->
        <?php if ($shippingBlock = $block->getLayout()->getBlock('checkout.cart.shipping')): ?>
            <div class="card bg-white p-6 rounded-xl shadow-sm border border-gray-200">
                <?= $shippingBlock->toHtml() ?>
            </div>
        <?php endif; ?>

        <!-- Cart Summary (Totals & Checkout Button) -->
        <div class="cart-summary card bg-white p-6 rounded-xl shadow-sm border border-gray-200">
            <?= $block->getChildHtml('cart.summary') ?>
        </div>
    </div>
</div>
