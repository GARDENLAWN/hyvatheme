<?php
declare(strict_types=1);

global $recaptcha, $formId;
/**
 * Hyvä Themes - https://hyva.io
 * Copyright © Hyvä Themes 2020-present. All rights reserved.
 * This product is licensed per Magento install
 * See https://hyva.io/license
 */

use Hyva\Theme\Model\ViewModelRegistry;
use Hyva\Theme\ViewModel\HyvaCsp;
use Hyva\Theme\ViewModel\ReCaptcha;
use Magento\Framework\Escaper;
use Magento\Framework\View\Element\Template;

/** @var Template $block */
/** @var Escaper $escaper */
/** @var ViewModelRegistry $viewModels */
/** @var HyvaCsp $hyvaCsp */

/** Generated with hyva-csp-helper.php */
?>
<script>
        function initContactForm() {
            return {
                errors: 0,
                hasCaptchaToken: 0,
                submitForm() {
                    // Do not rename $form, the variable is expected to be declared in the recaptcha output
                    const $form = document.querySelector('#<?= $escaper->escapeJs($formId) ?>');
                    <?= $recaptcha ? $recaptcha->getValidationJsHtml(ReCaptcha::RECAPTCHA_FORM_ID_CONTACT) : '' ?>

                    if (this.errors === 0) {
                        $form.submit();
                    }
                }
            }
        }
    </script>
<?php $hyvaCsp->registerInlineScript() ?>

<script>
function magento_ContactTemplatesFormMovedScript0() {
    const el = this.$el,
          wire = this.$wire;

    // @todo Implement interaction with the original code ^^
<?php /** Variables from "./Magento_Contact/templates/form.phtml" */ ?>
<?php /** References (`$formId`, `$recaptcha`, `$recaptcha->getValidationJsHtml(...)`) */ ?>

    return {
        init() {}
    }
}
window.addEventListener('alpine:init', () => Alpine.data('magento_ContactTemplatesFormMovedScript0', magento_ContactTemplatesFormMovedScript0), {once: true});
</script>
<?php $hyvaCsp->registerInlineScript() ?>
<script>
            //document.getElementById('google-map')
            //    .appendChild(document.getElementsByClassName('amgooglemap_mapblock')[0]);
        </script>
<?php $hyvaCsp->registerInlineScript() ?>

<script>
    <?php /** Generated with hyva-csp-helper.php */ ?>

    // Poprawiony format: używamy operatora spread, aby rozwinąć funkcję fabryczną.
    function gardenLawnHyvathemeForm() {
        // 1. Zdefiniuj obiekt bazowy, wywołując funkcję fabryczną initContactForm.
        const baseData = initContactForm();

        // 2. Połącz dane za pomocą operatora spread.
        return {
            ...baseData,
            // 3. Dodaj funkcję submit, która wywołuje submitForm z baseData
            // (to jest funkcja, której brakuje w oryginalnym PHTML)
            gardenLawnHyvathemeFormSubmitPrevent2(event) {
                // Użycie submitForm, które jest teraz częścią tego obiektu danych
                return this.submitForm();
            },
        }
    }
    window.addEventListener('alpine:init', () => Alpine.data('gardenLawnHyvathemeForm', gardenLawnHyvathemeForm), {once: true});
</script>
<?php $hyvaCsp->registerInlineScript() ?>
